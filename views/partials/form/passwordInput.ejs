<label>
    <span class="label">
        <%= inputOptions.name %>
    </span>
    <input name="<%=inputOptions.name %>" type="password" value="<%= (typeof inputOptions.defaultValue === "string" ? inputOptions.defaultValue : "") %>" <%= (inputOptions.required ? "required" : "") %>>
</label>