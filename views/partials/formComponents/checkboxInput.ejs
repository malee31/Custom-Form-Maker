<div class="pseudo-label">
    <span class="sub-label">
        <%= inputOptions.displayName %>
    </span>
	<% inputOptions.choices.forEach( choice => { %>
		<label class="inline-label">
			<span class="sub-label">
					<%= choice.value %>
			</span>
			<input data-columnname="<%= inputOptions.name %>" name="<%= inputOptions.name %>" type="checkbox" value="<%= choice.value %>" <%= (choice.checked ? "checked" : "") %>>
		</label>
	<% }) %>
</div>